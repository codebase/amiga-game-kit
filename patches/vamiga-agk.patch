diff --git a/Core/Components/Denise/Denise.cpp b/Core/Components/Denise/Denise.cpp
index 87200c4..8aa833f 100644
--- a/Core/Components/Denise/Denise.cpp
+++ b/Core/Components/Denise/Denise.cpp
@@ -122,6 +122,8 @@ Denise::setOption(Opt option, i64 value)
         case Opt::DENISE_FRAME_SKIPPING:
 
             config.frameSkipping = (isize)value;
+            // Apply immediately instead of after the pending skip run
+            if (frameSkips > config.frameSkipping) frameSkips = config.frameSkipping;
             return;
 
         case Opt::DENISE_HIDDEN_BITPLANES:
diff --git a/Core/Components/Memory/Memory.cpp b/Core/Components/Memory/Memory.cpp
index 1b7087b..a316d29 100644
--- a/Core/Components/Memory/Memory.cpp
+++ b/Core/Components/Memory/Memory.cpp
@@ -3095,6 +3095,9 @@ Memory::pokeCustom16(u32 addr, u16 value)
         case 0x1FC >> 1: // FMODE (AGA)
             agnus.pokeFMODE<s>(value); return;
         case 0x1FE >> 1: // NO-OP
+            // AGK: CPU writes of 0xA6E0, ptr hi, ptr lo hand a C string to the
+            // test harness (no-ops on real hardware)
+            if constexpr (s == Accessor::CPU) serialPort.agkHostWrite(value);
             copper.pokeNOOP(value); return;
     }
     
diff --git a/Core/Misc/RetroShell/Console.cpp b/Core/Misc/RetroShell/Console.cpp
index b661c82..75d53bd 100644
--- a/Core/Misc/RetroShell/Console.cpp
+++ b/Core/Misc/RetroShell/Console.cpp
@@ -1185,16 +1185,211 @@ Console::initCommonCommands(RSCommand &root)
             .flags  = vAmigaDOS ? rs::disabled : rs::hidden,
             .args   = {
                 { .name = { "seconds", "Delay" } },
-                { .name = { "unit", "Unit" }, .flags = rs::opt } // unused
+                { .name = { "unit", "Unit (seconds or frames)" }, .flags = rs::opt }
             },
             .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
-                
-                auto seconds = parseNum(args.at("seconds"));
-                shell.scheduleWakeup(SEC(seconds));
+
+                auto count = parseNum(args.at("seconds"));
+                auto unit = args.contains("unit") ? args.at("unit") : "seconds";
+
+                if (unit.rfind("tick", 0) == 0) {
+
+                    // Game frames (AGK ticks); time out after 4x as many video frames
+                    shell.awaitTicks(count, 4 * count + 250);
+
+                } else if (unit.rfind("frame", 0) == 0) {
+
+                    // Wake up at the start (v=0, h=0) of the count-th frame
+                    // boundary ahead, so input changes land frame-aligned
+                    auto toNext = agnus.pos.diff(0, 0);
+                    if (toNext <= 0) toNext = agnus.pos.cyclesPerFrame();
+                    auto rest = count > 1 ? agnus.pos.cyclesPerFrames(count - 1) : 0;
+                    shell.scheduleWakeup(DMA_CYCLES(toNext + rest));
+
+                } else {
+
+                    shell.scheduleWakeup(SEC(count));
+                }
                 throw ScriptInterruption();
             }
         });
         
+        //
+        // AGK harness extensions
+        //
+
+        root.add({
+
+            .tokens = { "waitserial" },
+            .chelp  = { "Pause the script until the serial log contains a text" },
+            .flags  = vAmigaDOS ? rs::disabled : rs::hidden,
+            .args   = {
+                { .name = { "text", "Text to wait for" } },
+                { .name = { "frames", "Timeout in frames" }, .flags = rs::opt }
+            },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                auto frames = args.contains("frames") ? parseNum(args.at("frames")) : 3000;
+                auto text = args.at("text");
+
+                // "hex:414243" spells any text, including '=' and quotes, which
+                // the argument parser would otherwise interpret
+                if (text.rfind("hex:", 0) == 0) {
+
+                    string decoded;
+                    for (usize i = 4; i + 1 < text.size(); i += 2) {
+                        decoded += char(std::stoi(text.substr(i, 2), nullptr, 16));
+                    }
+                    text = decoded;
+                }
+                if (text.empty()) throw CoreError(CoreError::OPT_INV_ARG, "empty text");
+                shell.awaitSerial(text, frames);
+                throw ScriptInterruption();
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk" },
+            .ghelp  = { "Amiga Game Kit harness helpers" },
+            .flags  = vAmigaDOS ? rs::disabled : rs::hidden
+        });
+
+        root.add({
+
+            .tokens = { "agk", "sync" },
+            .chelp  = { "Match waitserial on game ticks (on) or video frames (off)" },
+            .args   = { { .name = { "mode", "on or off" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                shell.tickSync = parseBool(args.at("mode"));
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "mouse" },
+            .chelp  = { "Move the mouse in port 1 by dx,dy (relative), each given +10000 (RetroShell reads '-' as a flag)" },
+            .args   = {
+                { .name = { "dx", "Horizontal delta" } },
+                { .name = { "dy", "Vertical delta" } }
+            },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                emulator.put(Command(Cmd::MOUSE_MOVE_REL, CoordCmd {
+                    .port = 0, .x = double(parseNum(args.at("dx")) - 10000), .y = double(parseNum(args.at("dy")) - 10000) }));
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "screenshot" },
+            .chelp  = { "Write the current frame as raw RGB without exiting" },
+            .args   = { { .name = { "path", "File path" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                auto path = host.makeAbsolute(args.at("path"));
+                std::ofstream file(path, std::ios::binary);
+                if (!file.is_open()) throw IOError(IOError::FILE_CANT_CREATE, path);
+                amiga.regressionTester.dumpTexture(amiga, file);
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "audio" },
+            .ghelp  = { "Record Paula's output (mono, 16 bit, 44.1 kHz)" },
+            .func   = [] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) { }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "audio", "start" },
+            .chelp  = { "Start recording (clears any earlier recording)" },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                if (agnus.clock > paula.audioClock) paula.executeUntil(agnus.clock);
+                audioPort.agkStart();
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "audio", "mark" },
+            .chelp  = { "Remember the current position of the recording under a name" },
+            .args   = { { .name = { "name", "Mark name" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                // Paula synthesizes lazily: bring the recording up to now first
+                if (agnus.clock > paula.audioClock) paula.executeUntil(agnus.clock);
+                audioPort.agkMarks.push_back({ args.at("name"), isize(audioPort.agkCapture.size()) });
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "audio", "save" },
+            .chelp  = { "Write the recording so far as a WAV file (keeps recording)" },
+            .args   = { { .name = { "path", "File path" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                if (agnus.clock > paula.audioClock) paula.executeUntil(agnus.clock);
+                auto path = host.makeAbsolute(args.at("path"));
+                std::ofstream file(path, std::ios::binary);
+                if (!file.is_open()) throw IOError(IOError::FILE_CANT_CREATE, path);
+                auto &pcm = audioPort.agkCapture;
+                u32 rate = u32(AudioPort::agkCaptureRate), bytes = u32(pcm.size() * 2);
+                auto le32 = [&](u32 v) { for (int i = 0; i < 4; i++) file.put(char((v >> (8 * i)) & 0xFF)); };
+                auto le16 = [&](u16 v) { file.put(char(v & 0xFF)); file.put(char(v >> 8)); };
+                file.write("RIFF", 4); le32(36 + bytes); file.write("WAVEfmt ", 8);
+                le32(16); le16(1); le16(1); le32(rate); le32(rate * 2); le16(2); le16(16);
+                file.write("data", 4); le32(bytes);
+                for (auto s : pcm) le16(u16(s));
+                std::ofstream marks(path.string() + ".marks");
+                for (auto &m : audioPort.agkMarks) marks << m.first << " " << m.second << "\n";
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "serial" },
+            .chelp  = { "Write everything the Amiga sent over serial to a file" },
+            .args   = { { .name = { "path", "File path" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                auto path = host.makeAbsolute(args.at("path"));
+                std::ofstream file(path, std::ios::binary);
+                if (!file.is_open()) throw IOError(IOError::FILE_CANT_CREATE, path);
+                file << serialPort.agkLog;
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "snapsave" },
+            .chelp  = { "Save a snapshot of the whole machine" },
+            .args   = { { .name = { "path", "File path" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                amiga.saveSnapshot(host.makeAbsolute(args.at("path")));
+            }
+        });
+
+        root.add({
+
+            .tokens = { "agk", "snapload" },
+            .chelp  = { "Restore a snapshot of the whole machine" },
+            .args   = { { .name = { "path", "File path" } } },
+            .func   = [this] (std::ostream &os, const Arguments &args, const std::vector<isize> &values) {
+
+                // Power on first so that running afterwards doesn't cold-boot
+                // over the restored state
+                emulator.powerOn();
+                amiga.loadSnapshot(host.makeAbsolute(args.at("path")));
+                emulator.run();
+            }
+        });
+
         root.add({
             
             .tokens = { "shutdown" },
diff --git a/Core/Misc/RetroShell/RetroShell.cpp b/Core/Misc/RetroShell/RetroShell.cpp
index fc6320d..6671936 100644
--- a/Core/Misc/RetroShell/RetroShell.cpp
+++ b/Core/Misc/RetroShell/RetroShell.cpp
@@ -374,9 +374,119 @@ RetroShell::setStream(std::ostream &os)
     console.setStream(os);
 }
 
+void
+RetroShell::scheduleNextFrameStart()
+{
+    // Poll at frame boundaries (v=0, h=0), not "one frame from now": then when
+    // a check sees new serial output doesn't depend on when the wait began
+    // (which varies with boot timing, e.g. between Kickstart and AROS).
+    auto toNext = agnus.pos.diff(0, 0);
+    if (toNext <= 0) toNext = agnus.pos.cyclesPerFrame();
+    scheduleWakeup(DMA_CYCLES(toNext));
+}
+
+void
+RetroShell::awaitSerial(const string &pattern, isize frames)
+{
+    awaitPattern = pattern;
+    awaitFrames = frames;
+    scheduleNextFrameStart();
+}
+
+void
+RetroShell::awaitTicks(isize ticks, isize timeoutFrames)
+{
+    awaitTickTarget = serialPort.agkTicks + ticks;
+    awaitFrames = timeoutFrames;
+    scheduleNextFrameStart();
+}
+
+void
+RetroShell::onAgkTick()
+{
+    bool resume = false;
+
+    if (awaitTickTarget >= 0 && serialPort.agkTicks >= awaitTickTarget) {
+
+        awaitTickTarget = -1;
+        resume = true;
+    }
+    if (tickSync && !awaitPattern.empty()) {
+
+        auto pos = serialPort.agkLog.find(awaitPattern, awaitOffset);
+        if (pos != string::npos) {
+
+            awaitOffset = pos + awaitPattern.size();
+            awaitPattern.clear();
+            resume = true;
+        }
+    }
+    if (resume) {
+
+        // Run the script now, synchronously, at the start of the game frame
+        cancelWakeup();
+        exec();
+    }
+}
+
 void
 RetroShell::serviceEvent()
 {
+    if (awaitTickTarget >= 0) {
+
+        // Fallback timer while waiting for ticks: only detects a stuck game
+        if (--awaitFrames > 0) {
+
+            scheduleNextFrameStart();
+            return;
+        }
+        *this << "waitticks: timeout - the game stopped marking frames (crashed, hung, or not calling agkPerfBegin)\n";
+        auto &log = serialPort.agkLog;
+        auto tail = log.size() > 1000 ? log.substr(log.size() - 1000) : log;
+        std::erase(tail, '\r');
+        *this << "waitserial: serial log tail (" << isize(log.size()) << " bytes total):\n";
+        *this << "<<<\n" << tail << "\n>>>\n";
+        awaitTickTarget = -1;
+        cancelWakeup();
+        commands.clear();
+        msgQueue.put(Msg::RSH_ERROR, objid);
+        return;
+    }
+
+    if (!awaitPattern.empty()) {
+
+        auto &log = serialPort.agkLog;
+        // In tick mode only ticks may match (determinism); frames just time out
+        auto pos = tickSync ? string::npos : log.find(awaitPattern, awaitOffset);
+
+        if (pos == string::npos) {
+
+            if (--awaitFrames > 0) {
+
+                // Not there yet: check again one frame later
+                scheduleNextFrameStart();
+                return;
+            }
+
+            *this << "waitserial: timeout waiting for '" << awaitPattern << "'\n";
+
+            // Show what the Amiga did print; it's usually the best clue
+            auto tail = log.size() > 1000 ? log.substr(log.size() - 1000) : log;
+            std::erase(tail, '\r'); // the console treats CR as "clear line"
+            *this << "waitserial: serial log tail (" << isize(log.size()) << " bytes total):\n";
+            *this << "<<<\n" << tail << "\n>>>\n";
+            awaitPattern.clear();
+            cancelWakeup();
+            commands.clear();
+            msgQueue.put(Msg::RSH_ERROR, objid);
+            return;
+        }
+
+        // Later waits only match output that arrives after this match
+        awaitOffset = pos + awaitPattern.size();
+        awaitPattern.clear();
+    }
+
     emulator.put(Command(Cmd::RSH_EXECUTE));
     cancelWakeup();
 }
diff --git a/Core/Misc/RetroShell/RetroShell.h b/Core/Misc/RetroShell/RetroShell.h
index 3d7c834..ac922c8 100644
--- a/Core/Misc/RetroShell/RetroShell.h
+++ b/Core/Misc/RetroShell/RetroShell.h
@@ -133,6 +133,23 @@ public:
      */
     void scheduleWakeup(Cycle delay);
     void cancelWakeup();
+
+    // AGK: 'waitserial' pauses the script until the serial log contains a
+    // pattern (checked once per frame) or the frame budget runs out
+    string awaitPattern;
+    isize awaitFrames = 0;
+    usize awaitOffset = 0;
+    void awaitSerial(const string &pattern, isize frames);
+    void scheduleNextFrameStart();
+
+    // AGK tick sync: the game marks the start of each frame of its main loop
+    // (a NOOP write). In tick mode, waits count game frames and resume the
+    // script right at a tick - just before the game reads its input - so
+    // results don't depend on how long the game's frame work takes.
+    bool tickSync = false;
+    i64 awaitTickTarget = -1;
+    void awaitTicks(isize ticks, isize timeoutFrames);
+    void onAgkTick();
     
     
     //
diff --git a/Core/Ports/AudioPort.cpp b/Core/Ports/AudioPort.cpp
index ca68727..e94b1af 100644
--- a/Core/Ports/AudioPort.cpp
+++ b/Core/Ports/AudioPort.cpp
@@ -358,10 +358,12 @@ AudioPort::synthesize(Cycle clock, Cycle target)
     if (amiga.objid != 0) return;
 
     // Run the ASR algorithm (adaptive sample rate)
-    if (config.asr) { updateSampleRateCorrection(); } else { sampleRateCorrection = 0.0; }
+    if (config.asr && !agkCapturing) { updateSampleRateCorrection(); } else { sampleRateCorrection = 0.0; }
     
     // Determine the number of elapsed cycles per audio sample
-    double cps = double(amiga.masterClockFrequency()) / (sampleRate + sampleRateCorrection);
+    // (AGK: a fixed rate while capturing, so the recording is deterministic)
+    double rate = agkCapturing ? agkCaptureRate : sampleRate + sampleRateCorrection;
+    double cps = double(amiga.masterClockFrequency()) / rate;
     
     // Determine how many samples we need to produce
     double exact = (double)(target - clock) / cps + fraction;
@@ -412,6 +414,7 @@ AudioPort::synthesize(Cycle clock, long count, double cyclesPerSample)
 
             // Fill with zeroes
             for (isize i = 0; i < count; i++) stream.write( SamplePair { 0, 0 } );
+            if (agkCapturing && count > 0) agkCapture.insert(agkCapture.end(), count, 0);
             stats.idleSamples += count;
             stream.mutex.unlock();
             return;
@@ -424,6 +427,7 @@ AudioPort::synthesize(Cycle clock, long count, double cyclesPerSample)
             if (std::abs(latest.l) + std::abs(latest.r) < 1e-8) {
                 
                 for (isize i = 0; i < count; i++) stream.write(SamplePair{});
+                if (agkCapturing && count > 0) agkCapture.insert(agkCapture.end(), count, 0);
                 stats.idleSamples += count;
                 stream.mutex.unlock();
                 return;
@@ -490,6 +494,7 @@ AudioPort::synthesize(Cycle clock, long count, double cyclesPerSample)
 
         // Write sample into ringbuffer
         stream.put( SamplePair { float(l), float(r) } );
+        agkRecord(l, r);
 
         cycle += cyclesPerSample;
     }
@@ -534,7 +539,8 @@ AudioPort::handleBufferOverflow()
     lastAlignment = utl::Time::now();
 
     // Adjust the sample rate if the emulator runs under normal conditions
-    if (emulator.isRunning() && !emulator.isWarping()) {
+    // (AGK: not while capturing - nobody drains the stream then)
+    if (emulator.isRunning() && !emulator.isWarping() && !agkCapturing) {
 
         stats.bufferOverflows++;
         logmsg(LOG_AUDBUF, "Audio buffer overflow after %f seconds\n", elapsedTime.asSeconds());
diff --git a/Core/Ports/AudioPort.h b/Core/Ports/AudioPort.h
index 92790b9..9592819 100644
--- a/Core/Ports/AudioPort.h
+++ b/Core/Ports/AudioPort.h
@@ -138,6 +138,22 @@ public:
     // Detector for measuring the sample rate
     SampleRateDetector detector = SampleRateDetector(amiga);
 
+    // AGK: while capturing, every produced sample (mono mix, 16 bit) is kept
+    // here at a fixed rate, independent of the host's audio buffer.
+    bool agkCapturing = false;
+    std::vector<i16> agkCapture;
+    std::vector<std::pair<string, isize>> agkMarks;  // name, sample index
+    static constexpr double agkCaptureRate = 44100.0;
+    void agkStart() {
+        agkCapture.clear();
+        agkMarks.clear();
+        fraction = 0.0;     // not in snapshots: reset so recordings are identical
+        agkCapturing = true;
+    }
+    void agkRecord(double l, double r) {
+        if (agkCapturing) agkCapture.push_back(i16(std::clamp((l + r) * 0.5, -1.0, 1.0) * 32767.0));
+    }
+
 
     //
     // Methods
diff --git a/Core/Ports/SerialPort.cpp b/Core/Ports/SerialPort.cpp
index b8a0833..9e811a3 100644
--- a/Core/Ports/SerialPort.cpp
+++ b/Core/Ports/SerialPort.cpp
@@ -303,6 +303,37 @@ SerialPort::recordIncomingByte(int byte)
     }
 }
 
+void
+SerialPort::agkHostWrite(u16 value)
+{
+    switch (agkHostState) {
+
+        case 0: // Waiting for the magic word
+            if (value == 0xA6E0) agkHostState = 1;
+            if (value == 0xA6E1) { agkTicks++; retroShell.onAgkTick(); }
+            break;
+
+        case 1: // Upper half of the string address
+            agkHostPtr = u32(value) << 16;
+            agkHostState = 2;
+            break;
+
+        default: // Lower half: copy the NUL-terminated string into the log
+        {
+            agkHostPtr |= value;
+            agkHostState = 0;
+            {   SYNCHRONIZED
+                for (isize i = 0; i < 4096; i++) {
+                    auto c = mem.spypeek8<Accessor::CPU>(u32(agkHostPtr + i));
+                    if (c == 0) break;
+                    agkLog += char(c);
+                }
+            }
+            break;
+        }
+    }
+}
+
 void
 SerialPort::recordOutgoingByte(int byte)
 {
@@ -312,6 +343,7 @@ SerialPort::recordOutgoingByte(int byte)
 
         // Record the incoming byte
         outgoing += char(byte);
+        agkLog += char(byte);
 
         // Inform the GUI if the record buffer had been empty
         if (outgoing.length() == 1) msgQueue.put(Msg::SER_OUT);
diff --git a/Core/Ports/SerialPort.h b/Core/Ports/SerialPort.h
index db191e4..e7197e6 100644
--- a/Core/Ports/SerialPort.h
+++ b/Core/Ports/SerialPort.h
@@ -57,6 +57,24 @@ public:
     std::u16string incoming;
     std::u16string outgoing;
 
+public:
+
+    // AGK: every byte ever sent by the Amiga (not consumed by the GUI)
+    std::string agkLog;
+
+    // AGK host channel: strings handed over via writes to NOOP ($DFF1FE)
+    void agkHostWrite(u16 value);
+
+    // AGK: number of game-frame marks (ticks) seen
+    i64 agkTicks = 0;
+
+private:
+
+    isize agkHostState = 0;
+    u32 agkHostPtr = 0;
+
+public:
+
 
     //
     // Methods
